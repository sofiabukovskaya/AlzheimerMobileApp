<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alzheimermobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlzheimerMobile">
        <activity android:name=".NotesActivity" />
        <activity android:name=".AddNoteActivity"></activity>
        <activity android:name=".DoctorProfileActivity" />
        <activity android:name=".ToDoActivity" /> <!-- <activity -->
        <!-- android:name=".oDoActivity" -->
        <!-- android:label="@string/title_activity_to_do" -->
        <!-- android:theme="@style/Theme.AlzheimerMobile.NoActionBar" /> -->
        <!-- <activity android:name=".ToDoActivty" /> -->
        <activity android:name=".TestActivity" />
        <activity android:name=".ProfileUserActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".AboutActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>